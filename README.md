Sales forecasting overview

Run the dashboard with:

```bash
streamlit run streamlit_app.py
```

The app reads `train.csv` from the project root and reproduces the notebook's overview, forecast, anomaly, and clustering views.
